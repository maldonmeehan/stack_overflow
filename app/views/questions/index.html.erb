<h1>Questions</h1>
<ul>
  <% @questions.each do |question| %>
    <li><%= link_to question.content, question_path(question)%> | by <%= question.user %></li>
  <% end %>
</ul>

<%= link_to "New Question", new_question_path %>
