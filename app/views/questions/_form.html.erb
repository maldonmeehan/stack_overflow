<%= render 'layouts/errors', :object => @question %>

<%= form_for @question do |f| %>
  <%= f.label :user %>
  <%= f.text_field :user %>
  <%= f.label :content %>
  <%= f.text_area :content %>
  <%= f.submit %>
<% end %>
